# Grid Trading Service Configuration
SERVER_PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=grid_trading
DB_SSL_MODE=disable

# Order Assurance Service
ORDER_ASSURANCE_URL=http://localhost:9090

# Sync Job Configuration
SYNC_JOB_ENABLED=true
SYNC_JOB_CRON=0 * * * *

# ===========================
# Order Assurance Service Configuration
# ===========================

# Order Assurance Server Port (comment out if not running order-assurance)
# SERVER_PORT=9090

# Binance API Configuration
BINANCE_API_KEY=your_binance_api_key
BINANCE_API_SECRET=your_binance_api_secret
BINANCE_TESTNET=false  # Set to true for testnet

# Grid Trading Service URL (for webhooks)
GRID_TRADING_URL=http://localhost:8080

# Monitor Interval (seconds) - how often to check orders for fills
MONITOR_INTERVAL=30